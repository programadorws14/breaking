import { StyleSheet, Text, View } from 'react-native';

const styles = StyleSheet.create({
    header: {
        marginTop: 20,
        marginBottom: 10
    },

    titlePrimary:{
        textAlign: 'center',
        fontSize: 40,
        fontWeight: 'bold',
        color: '#FFF'
    },
    titleSecondary: {
        textAlign: 'center',
        color: '#026723',
        fontSize: 22,
        fontWeight: 'bold',
    }
});

export default styles ;